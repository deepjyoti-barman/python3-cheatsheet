# Asterisk (*) as unpacking / splat operator (iterables)
# When placed right before a list, tuple, or iterable, it "unpacks" its individual elements
# In this context, developers frequently call it the splat or unpacking operator
numbers = [1, 2, 3]
print(*numbers)  # 1 2 3 (Prints elements separated by spaces)
